apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayzio-redis
  labels:
    app: stayzio-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stayzio-redis
  template:
    metadata:
      labels:
        app: stayzio-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
              name: redis
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: stayzio-redis-service
spec:
  selector:
    app: stayzio-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
